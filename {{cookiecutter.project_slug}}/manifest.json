{
  "name": "{{ cookiecutter.project_name }}",
  "description": "{{ cookiecutter.project_short_description.replace('\"', '\\\"') }}",
  "version": "{{ cookiecutter.version }}",
  "permissions": [
    "activeTab"
  ],
  "background": {
    "scripts": ["config.js", "dist/background.js"],
    "persistent": false
  },
  "manifest_version": 2
}
